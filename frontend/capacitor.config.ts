import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pomodorotable.app',
  appName: 'PomodoroTable',
  webDir: 'app-dist'
};

export default config;
